# AWS Configuration Template
# Copy this file to .env and fill in your AWS credentials
# DO NOT commit .env file to version control!

# AWS Credentials
# Option 1: Use AWS Access Keys
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here

# Option 2: Use AWS Session Token (for temporary credentials)
# AWS_SESSION_TOKEN=your_session_token_here

# AWS Region
AWS_REGION=us-east-1

# S3 Configuration
S3_BUCKET_NAME=code-review-bucket
S3_PREFIX=code-snapshots/

# ============================================================
# SETUP INSTRUCTIONS
# ============================================================

# 1. Create AWS Account
#    Visit: https://aws.amazon.com/

# 2. Create IAM User with S3 Access
#    - Go to IAM Console
#    - Create user with programmatic access
#    - Attach policy: AmazonS3FullAccess (or custom policy)
#    - Save Access Key ID and Secret Access Key

# 3. Set Environment Variables
#    
#    Windows (PowerShell):
#      $env:AWS_ACCESS_KEY_ID="your_key"
#      $env:AWS_SECRET_ACCESS_KEY="your_secret"
#      $env:S3_BUCKET_NAME="your-bucket-name"
#
#    Windows (CMD):
#      set AWS_ACCESS_KEY_ID=your_key
#      set AWS_SECRET_ACCESS_KEY=your_secret
#      set S3_BUCKET_NAME=your-bucket-name
#
#    Linux/Mac:
#      export AWS_ACCESS_KEY_ID="your_key"
#      export AWS_SECRET_ACCESS_KEY="your_secret"
#      export S3_BUCKET_NAME="your-bucket-name"

# 4. Alternative: AWS CLI Configuration
#    Install AWS CLI: pip install awscli
#    Run: aws configure
#    This will create ~/.aws/credentials and ~/.aws/config
#    boto3 will automatically use these credentials

# ============================================================
# SECURITY BEST PRACTICES
# ============================================================

# ❌ NEVER commit credentials to Git
# ✅ Use environment variables
# ✅ Use IAM roles when running on AWS (EC2, Lambda, ECS)
# ✅ Use temporary credentials when possible
# ✅ Rotate credentials regularly
# ✅ Use least privilege IAM policies

# ============================================================
# MINIMUM IAM POLICY FOR S3
# ============================================================

# {
#   "Version": "2012-10-17",
#   "Statement": [
#     {
#       "Effect": "Allow",
#       "Action": [
#         "s3:PutObject",
#         "s3:GetObject",
#         "s3:DeleteObject",
#         "s3:ListBucket",
#         "s3:CreateBucket",
#         "s3:HeadBucket"
#       ],
#       "Resource": [
#         "arn:aws:s3:::code-review-bucket",
#         "arn:aws:s3:::code-review-bucket/*"
#       ]
#     }
#   ]
# }

# ============================================================
# MOCK MODE (For Testing Without AWS)
# ============================================================

# If credentials are not provided, the system automatically
# uses MOCK mode with sample code.
#
# To use mock mode:
# - Simply don't set AWS credentials
# - The system will warn you and use mock data
